import sys
from pathlib import Path

mode = int(sys.argv[4])
counts = int(sys.argv[3])
user_path = sys.argv[1]
dir_name = sys.argv[2]

for i in range(1, counts + 1):
    Path(user_path + "/" + dir_name + str(i)).mkdir(mode)
